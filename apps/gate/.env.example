# Copy to .env (gitignored) for a local build, or set these in the environment of
# `pnpm --filter gate build` — Vite bakes them into the bundle at build time.

# The api origin this scanner calls. Defaults to http://localhost:8787 when unset.
VITE_API_BASE_URL=https://api.fuda.sh
