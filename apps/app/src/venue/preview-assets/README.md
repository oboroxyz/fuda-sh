# Preview logo

`wassie-coffee.jpg` is the unchanged 400 × 400 logo referenced by the staff mockup's `store-onboarding.html` at `/src/mocks/onboarding/wassie-coffee.jpg`. Imported at the user's explicit request for this preview; no mockup implementation code was copied.

It is imported only by the opt-in development preview, not served from `public/`. Production issuer logos continue to come from the API.
