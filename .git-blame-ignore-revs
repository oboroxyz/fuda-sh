# Commits that changed formatting only. `git blame` skips these and reports the
# revision that last changed the content itself.
#
# Use `git config blame.ignoreRevsFile .git-blame-ignore-revs` once per clone;
# GitHub picks the file up automatically.

# style: unwrap markdown prose repo-wide
cd863d7834ac4af94e6e98d967b12969d3707424
